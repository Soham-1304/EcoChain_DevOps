apiVersion: v1
kind: ConfigMap
metadata:
  name: ecochain-config
  namespace: ecochain
data:
  FLASK_ENV: "production"
  DATABASE_HOST: "postgres-service"
  DATABASE_PORT: "5432"
  DATABASE_NAME: "ecochain"
